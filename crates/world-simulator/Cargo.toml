[package]
name = "world-simulator"
version = "0.1.1"
edition = "2021"
license.workspace = true
default-run = "world-simulator"

[dependencies]
world-core = { path = "../world-core" }
world-crypto = { path = "../world-crypto" }
world-protocol = { path = "../world-protocol" }
